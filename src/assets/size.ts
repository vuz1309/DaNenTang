import {scale} from 'react-native-size-matters';

const Size = {
  FONTSIZE: {
    small: scale(12),
    normal: scale(14),
    large: scale(16),
  },
  PADDING: {
    defaultTextPadding: 4,
    defaultButtonPadding: 4,
  },
};

export default Size;
